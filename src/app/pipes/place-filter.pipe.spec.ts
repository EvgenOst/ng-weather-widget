import { PlaceFilterPipePipe } from './place-filter.pipe';

describe('PlaceFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
